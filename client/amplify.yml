version: 1
frontend:
  phases:
  preBuild:
    commands:
      - npm install
    build:
      commands:
        - cd client
        - npm run build
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []
